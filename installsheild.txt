#====[ Script Settings ]====#

$action = "Install"
$SourceApp = "wsftp_12.9.0_English_SNB7JHMJ7MTC8CA4JJ1D0TIRS.exe"
$ArgumentList = @(
    "-s"
)

#=======[ Execution ]=======#

$Process = Start-Process -FilePath (Join-Path $PSScriptRoot $SourceApp) -ArgumentList $ArgumentList -NoNewWindow -Wait -PassThru

if ($Process.ExitCode -eq 0 -or $Process.ExitCode -eq 3010) {
    Write-Host "${action}ed $SourceApp successfully"
    Exit 0
}
else {
    Write-Host "$action failed with exit code $($Process.ExitCode)"
    Exit $Process.ExitCode
}


---------------------------------------------------------------------------------------------------
$InstallPath = "C:\Program Files (x86)\Progress\WS_FTP Professional"
$ExePath = "C:\Program Files (x86)\Progress\WS_FTP Professional\wsftppro.exe"

if ((Test-Path $InstallPath) -and (Test-Path $ExePath)) {
    Write-Host "WS_FTP Professional detected"
    Exit 0
}
else {
    Write-Host "WS_FTP Professional not detected"
    Exit 1
}
-----------------------------------------------------------------------------------------------------
#====[ Script Settings ]====#

$action = "Install"
$SourceApp = "AltairMonarchStandaloneInstallerEn_2024.1.exe"
$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "AltairMonarch2024_$($action).log"

$ArgumentList = @(
    "/quiet"
    "/norestart"
    "/silent"
)

#=======[ Execution ]=======#

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$InstallerPath = Join-Path $PSScriptRoot $SourceApp

if (!(Test-Path $InstallerPath)) {
    Write-Host "Installer not found: $InstallerPath"
    Stop-Transcript
    Exit 1
}

Write-Host "Launching installer: $InstallerPath"

$Process = Start-Process -FilePath $InstallerPath -ArgumentList $ArgumentList -Wait -PassThru

Write-Host "Exit code: $($Process.ExitCode)"

if ($Process.ExitCode -eq 0) {
    Write-Host "Installed $SourceApp successfully"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "Install failed with exit code $($Process.ExitCode)"
    Stop-Transcript
    Exit $Process.ExitCode
}


#====[ Script Settings ]====#

$action = "Uninstall"
$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "AltairMonarch2024_$($action).log"

$ArgumentList = @(
    "/quiet"
    "/norestart"
    "/silent"
)

#=======[ Execution ]=======#

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$App = Get-ItemProperty -Path `
    "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*",
    "HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*" `
    -ErrorAction SilentlyContinue |
    Where-Object { $_.DisplayName -like "*Altair Monarch*2024*" } |
    Select-Object -First 1

if (!$App) {
    Write-Host "Altair Monarch 2024 not found"
    Stop-Transcript
    Exit 0
}

Write-Host "Uninstalling: $($App.DisplayName)"

$Process = Start-Process -FilePath $App.UninstallString -ArgumentList $ArgumentList -Wait -PassThru

Write-Host "Exit code: $($Process.ExitCode)"

if ($Process.ExitCode -eq 0) {
    Write-Host "Uninstalled successfully"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "Uninstall failed with exit code $($Process.ExitCode)"
    Stop-Transcript
    Exit $Process.ExitCode
}


$App = Get-ItemProperty -Path `
    "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*",
    "HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*" `
    -ErrorAction SilentlyContinue |
    Where-Object { $_.DisplayName -like "*Altair Monarch*2024*" } |
    Select-Object -First 1

if ($App) {
    Write-Host "Detected: $($App.DisplayName)"
    Exit 0
} else {
    Exit 1
}




---------------------------------------------------------------------------------------------------------------------------------------------------------

#====[ Script Settings ]====#

$action = "Install"
$SourceApp = "UiPath Studio v2025.10.7.msi"
$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "UiPathStudio2025_$($action).log"

$ArgumentList = @(
    "/i `"$SourceApp`""
    "/quiet"
    "/norestart"
)

#=======[ Execution ]=======#

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$InstallerPath = Join-Path $PSScriptRoot $SourceApp

if (!(Test-Path $InstallerPath)) {
    Write-Host "Installer not found: $InstallerPath"
    Stop-Transcript
    Exit 1
}

Write-Host "Launching installer: $InstallerPath"

$Process = Start-Process -FilePath "msiexec.exe" -ArgumentList $ArgumentList -Wait -PassThru

Write-Host "Exit code: $($Process.ExitCode)"

if ($Process.ExitCode -eq 0) {
    Write-Host "Installed $SourceApp successfully"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "Install failed with exit code $($Process.ExitCode)"
    Stop-Transcript
    Exit $Process.ExitCode
}


#====[ Script Settings ]====#

$action = "Uninstall"
$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "UiPathStudio2025_$($action).log"

$ArgumentList = @(
    "/x `"$SourceApp`""
    "/quiet"
    "/norestart"
)

#=======[ Execution ]=======#

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$App = Get-ItemProperty -Path `
    "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*",
    "HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*" `
    -ErrorAction SilentlyContinue |
    Where-Object { $_.DisplayName -like "*UiPath Studio*2025*" } |
    Select-Object -First 1

if (!$App) {
    Write-Host "UiPath Studio 2025 not found"
    Stop-Transcript
    Exit 0
}

Write-Host "Uninstalling: $($App.DisplayName)"

$Process = Start-Process -FilePath "msiexec.exe" -ArgumentList $ArgumentList -Wait -PassThru

Write-Host "Exit code: $($Process.ExitCode)"

if ($Process.ExitCode -eq 0) {
    Write-Host "Uninstalled successfully"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "Uninstall failed with exit code $($Process.ExitCode)"
    Stop-Transcript
    Exit $Process.ExitCode
}


$App = Get-ItemProperty -Path `
    "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\*",
    "HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*" `
    -ErrorAction SilentlyContinue |
    Where-Object { $_.DisplayName -like "*UiPath Studio*2025*" } |
    Select-Object -First 1

if ($App) {
    Write-Host "Detected: $($App.DisplayName)"
    Exit 0
} else {
    Exit 1
}
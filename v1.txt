# Install-XMLSpyProf2026.ps1

$action = "Install"
$SourceApp = "XMLSpyProf2026_x64.exe"
$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "XMLSpy2026_$($action).log"

$ArgumentList = @(
    "/quiet"
    "/norestart"
)

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$InstallerPath = Join-Path $PSScriptRoot $SourceApp

if (!(Test-Path $InstallerPath)) {
    Write-Host "Installer not found: $InstallerPath"
    Stop-Transcript
    Exit 1
}

$Process = Start-Process -FilePath $InstallerPath -ArgumentList $ArgumentList -Wait -PassThru -NoNewWindow

if ($Process.ExitCode -eq 0) {
    Write-Host "Installed $SourceApp successfully"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "Install failed with exit code $($Process.ExitCode)"
    Stop-Transcript
    Exit $Process.ExitCode
}




# Detection-XMLSpyProf2026.ps1

$Path = "C:\Program Files\Altova\XMLSpy2026\XMLSpy.exe"

if (Test-Path $Path) {
    exit 0
} else {
    exit 1
}





# Uninstall-XMLSpyProf2026.ps1

$LogFolder = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs"
$LogFile = Join-Path $LogFolder "XMLSpy2026_Uninstall.log"

if (!(Test-Path $LogFolder)) {
    New-Item -ItemType Directory -Path $LogFolder -Force | Out-Null
}

Start-Transcript -Path $LogFile -Append -Force

$App = Get-ItemProperty `
    "HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*", `
    "HKLM:\Software\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\*" `
    -ErrorAction SilentlyContinue |
    Where-Object { $_.DisplayName -like "*XMLSpy*" } |
    Select-Object -First 1

if ($App) {
    $Uninstall = $App.UninstallString

    if ($Uninstall -match "MsiExec") {
        $Uninstall = $Uninstall -replace "/I", "/X"
        Start-Process "msiexec.exe" -ArgumentList "$($Uninstall -replace 'MsiExec.exe','') /quiet /norestart" -Wait
    } else {
        Start-Process cmd.exe -ArgumentList "/c `"$Uninstall`" /quiet /norestart" -Wait -NoNewWindow
    }

    Write-Host "Uninstall completed"
    Stop-Transcript
    Exit 0
} else {
    Write-Host "XMLSpy not found in registry - already uninstalled"
    Stop-Transcript
    Exit 0
}


$uninstallKey = Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall" |
    Where-Object { $_.GetValue("DisplayName") -like "*WS_FTP*" }
$SourceApp = $uninstallKey.GetValue("UninstallString"



---------------------------------------------------------------------------------------------
#=====[ Script Settings ]=====#
$action    = "Install"
$SourceApp = "TextPadSetupv9-x64.exe"
$LogPath   = "C:\ProgramData\Microsoft\IntuneManagementExtension\Logs\${SourceApp}_${action}.log"

$ArgumentList = @(
    "/VERYSILENT"
    "/NORESTART"
    "/LOG=`"$LogPath`""
)

#=====[ Execution ]=====#
$InstallerPath = Join-Path $PSScriptRoot $SourceApp

if (-not (Test-Path $InstallerPath)) {
    Write-Error "Installer not found: $InstallerPath"
    Exit 1
}

$process = Start-Process -FilePath $InstallerPath -ArgumentList $ArgumentList -Wait -NoNewWindow -PassThru

if ($process.ExitCode -ne 0) {
    Write-Error "$action failed for $SourceApp (Exit code: $($process.ExitCode))"
    Exit $process.ExitCode
}

#=====[ IL5 Compliance – Remove PortableWizard ]=====#
$PortableWizard = "C:\Program Files\TextPad\PortableWizard.exe"

try {
    if (Test-Path $PortableWizard) {
        Remove-Item $PortableWizard -Force -ErrorAction Stop
        Write-Host "PortableWizard.exe removed for IL5 compliance"
    }
} catch {
    Write-Error "FAILED to remove PortableWizard.exe - IL5 compliance not met. Aborting. $_"
    Exit 1
}

Write-Host "$action`ed $SourceApp successfully"
Exit 0
---------------------------------------------------------------------------------------------------------


​You​
CRforVS6413SP34_0-80007712.EXE /qn

CRforVS6413SP34_0-80007712.EXE REMOVE=ALL /qn


-------------------------------------------------------------------------------------------------------










